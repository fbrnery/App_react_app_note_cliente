import React, { Fragment } from 'react';
import { Link } from "react-router-dom";

function Login() {
    return (
      <div>
        <Fragment>
            <h1> Bem vindo, a página Login !!!</h1>
        </Fragment>
      </div>
    );
  }

export default Login;