import React, { Fragment } from 'react';
import { Link } from "react-router-dom";

function Register() {
    return (
      <div>
        <Fragment>
            <h1> Bem vindo, a página Register !!!</h1>
        </Fragment>
      </div>
    );
  }

export default Register;